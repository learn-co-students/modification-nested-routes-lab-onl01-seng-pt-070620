<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>
  <%= collection_select(:song, :artist_id, Artist.all, :id, :name) %><br>

  <br />

  <%= f.submit %>
<% end %>
